# Base URL of the backend API (versioned per spec: /api/v1)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Backend origin (no path) — used to resolve audio/media file URLs
NEXT_PUBLIC_MEDIA_BASE_URL=http://localhost:4000
